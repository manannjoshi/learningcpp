#include <iostream>
#include <cmath>

int main(){

    int a = 1;
    int b = 2;
    double z;

    //math functions
    //z = std::min(a,b);
    //z = std::max(a,b);
    //z = pow(2,10); // power 
    //z = abs(-3); //absolute value
    //z = sqrt(9); //squareroot
    //z = round(3.14);  //rounds
    //z = ceil(3.14);  //rounds up
    //z = floor(3.14); //rounds down

    return 0;
}